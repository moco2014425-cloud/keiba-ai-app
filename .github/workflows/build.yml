name: Flet Android Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Install Flet
      run: |
        pip install flet
        
    - name: Build Android APK
      run: |
        flet build apk main.py
        
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: keiba-ai-app-apk
        path: build/apk/*.apk
